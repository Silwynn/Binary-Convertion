def binary_to_decimal(binary):
    return int(binary, 2)

def decimal_to_hex_oct(decimal):
    return hex(decimal).lstrip("0x").upper(), oct(decimal).lstrip("0o")

def binary_operations_menu():
    choice = int(input("\nBinary Operations Menu:\n[1] Division\n[2] Multiplication\n[3] Subtraction\n[4] Addition\n[5] Negative (2's Complement)\nEnter your choice (1-5): "))
    binary_input = input("Enter the binary number: ").replace(" ", "")

    if choice == 5:
        negative_binary = bin(-int(binary_input, 2) & int('1' * len(binary_input), 2))[2:]
        print(f"Negative (2's Complement): {negative_binary}")
    else:
        print("Invalid choice. Please enter a valid option.")

def number_system_conversion_menu():
    choice = int(input("\nNumber System Conversion Menu:\n[1] Binary to X\n[2] Decimal to X\n[3] Octal to X\n[4] Hexa to X\nEnter your choice (1-4): "))
    binary_input = input("Enter the binary number: ").replace(" ", "")
    decimal_input = int(binary_input, 2)

    if choice == 1:
        decimal_output = binary_to_decimal(binary_input)
        hex_output, oct_output = decimal_to_hex_oct(decimal_output)
        print(f"\nDecimal: {decimal_output}\nHexa: {hex_output}\nOctal: {oct_output}")
    elif choice == 2:
        hex_output, oct_output = decimal_to_hex_oct(decimal_input)
        print(f"\nHexa: {hex_output}\nOctal: {oct_output}")
    elif choice == 3:
        decimal_output = int(str(decimal_input), 😎
        hex_output, _ = decimal_to_hex_oct(decimal_output)
        print(f"\nDecimal: {decimal_output}\nHexa: {hex_output}")
    elif choice == 4:
        decimal_output = int(str(decimal_input), 16)
        oct_output = oct(decimal_output).lstrip("0o")
        print(f"\nDecimal: {decimal_output}\nOctal: {oct_output}")
    else:
        print("Invalid choice. Please enter a valid option.")

def main_menu():
    while True:
        choice = int(input("\nMain Menu:\n[1] Binary Operations\n[2] Number System Conversion\n[3] Exit\nEnter your choice (1-3): "))

        if choice == 1:
            binary_operations_menu()
        elif choice == 2:
            number_system_conversion_menu()
        elif choice == 3:
            print("Exiting the program. Goodbye!")
            break
        else:
            print("Invalid choice. Please enter a valid option.")

if _name_ == "_main_":
    main_menu()



binary=StringVar()
    lbldecimal= int(binary, 2)
    lblhexa=hex(lbldecimal)[2:]
    lbloctal= oct(lbldecimal)[2:]